name: CI

on:
  pull_request:
    branches: [main, develop]
  push:
    branches: [main, develop]
  workflow_dispatch:
  release:
    types: [published]

permissions:
  contents: read

jobs:
  validate:
    name: Node.js ${{ matrix.node }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        node: [22, 24]
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.event_name == 'release' && github.event.release.tag_name || github.sha }}
          persist-credentials: false
      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: ${{ matrix.node }}
          cache: npm
      - name: Validate locked source and installed package
        run: |
          npm ci --ignore-scripts
          npm run format:check
          npm run check:version
          npm run typecheck
          npm test
          python3 -m unittest discover -s test -p local_rerank_test.py
          npm run test:package
      - name: Check public benchmark export and official scorer
        if: matrix.node == 22
        run: |
          python3 -m venv .srcx/benchmark-venv
          .srcx/benchmark-venv/bin/pip install -r eval/bm25-requirements.txt
          .srcx/benchmark-venv/bin/python -m unittest discover -s test -p benchmark_data_test.py
          .srcx/benchmark-venv/bin/python -m unittest discover -s test -p cosqa_bm25_test.py

  publish:
    needs: validate
    if: >-
      github.repository == 'lambdadb/srcx' &&
      (github.event_name == 'release' ||
       (github.event_name == 'push' && github.ref == 'refs/heads/develop' &&
        vars.NPM_DEV_PUBLISH_ENABLED == 'true'))
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
      id-token: write
    concurrency:
      # Never interrupt a registry write. New pending dev jobs replace older ones.
      group: npm-${{ github.event_name == 'release' && github.event.release.tag_name || 'dev' }}
      cancel-in-progress: false
    steps:
      - name: Check out verified source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.event_name == 'release' && github.event.release.tag_name || github.sha }}
          fetch-depth: 0
          persist-credentials: false
      - name: Set up publishing runtime
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          registry-url: https://registry.npmjs.org
          package-manager-cache: false
      - name: Require an OIDC-capable npm
        run: |
          node -e 'const {execFileSync}=require("node:child_process"); const v=execFileSync("npm",["--version"],{encoding:"utf8"}).trim().split(".").map(Number); if(v[0]<11 || (v[0]===11 && (v[1]<5 || (v[1]===5 && v[2]<1)))) throw Error("npm >=11.5.1 required");'
      - name: Prepare deterministic development version
        if: github.event_name == 'push'
        id: dev
        run: node scripts/dev-release.mjs prepare
      - name: Verify explicit release metadata and main ancestry
        if: github.event_name == 'release'
        id: release
        env:
          RELEASE_TAG: ${{ github.event.release.tag_name }}
          RELEASE_PRERELEASE: ${{ github.event.release.prerelease }}
        run: |
          git merge-base --is-ancestor HEAD origin/main
          node scripts/check-release.mjs --release
          node --input-type=module -e 'import fs from "node:fs"; const p=JSON.parse(fs.readFileSync("package.json","utf8")); if(p.version.includes("-dev.")) throw Error("Development versions publish through develop pushes, not GitHub Releases.");'
      - name: Pack once and test the exact publication artifact
        if: github.event_name == 'release' || steps.dev.outputs.ready == 'true'
        id: package
        shell: bash
        run: |
          npm ci --ignore-scripts
          npm run check:version
          npm pack --json --pack-destination "$RUNNER_TEMP" > "$RUNNER_TEMP/package.json"
          CLI_TARBALL=$(node --input-type=module -e 'import fs from "node:fs"; const file=JSON.parse(fs.readFileSync(process.env.RUNNER_TEMP+"/package.json","utf8"))[0].filename; console.log(`${process.env.RUNNER_TEMP}/${file}`);')
          npm run test:package -- "$CLI_TARBALL"
          echo "tarball=$CLI_TARBALL" >> "$GITHUB_OUTPUT"
      - name: Publish development version and verify registry state
        if: github.event_name == 'push' && steps.dev.outputs.ready == 'true'
        env:
          CLI_TARBALL: ${{ steps.package.outputs.tarball }}
        run: node scripts/dev-release.mjs publish
      - name: Publish explicit release using npm Trusted Publishing
        if: github.event_name == 'release'
        env:
          CLI_TARBALL: ${{ steps.package.outputs.tarball }}
          NPM_CHANNEL: ${{ steps.release.outputs.channel }}
        run: npm publish "$CLI_TARBALL" --access public --tag "$NPM_CHANNEL" --provenance
